commit e75b7bb8bd39d617a49cf4a4c0b3c2a6eb67b0d1
Author: LeonPark-gif <lgregarek@gmail.com>
Date:   Sun Mar 8 18:18:34 2026 +0100

    Erster Commit, hoffentlich explodiert nichts

diff --git a/src/store/useAppStore.ts b/src/store/useAppStore.ts
new file mode 100644
index 0000000..6473a26
--- /dev/null
+++ b/src/store/useAppStore.ts
@@ -0,0 +1,1418 @@
+import { create } from 'zustand';
+import type { StateCreator } from 'zustand';
+import { persist, createJSONStorage } from 'zustand/middleware';
+import type { StateStorage } from 'zustand/middleware';
+
+
+// --- Types ---
+export type ThemeColor = 'rose' | 'blue' | 'green' | 'purple' | 'orange' | 'cyan' | 'scifi' | 'mint' | 'peach' | 'lavender' | 'sky' | 'amber' | 'emerald' | 'violet' | 'gray' | 'black' | 'white' | 'stone' | 'slate';
+
+export interface User {
+    id: string;
+    name: string;
+    avatar: string; // Emoji
+    color: string; // Tailwind color class
+    isHidden?: boolean; // For Admin
+    displayOrder?: string[]; // IDs of widgets in order
+    widgetSizes?: Record<string, 'small' | 'medium' | 'large'>;
+    colorLabels?: Record<string, string | undefined>; // Allow undefined for safety
+    aiSettings?: AISettings; // New: AI Privacy & Feature Settings
+    chatHistory?: ChatMessage[]; // Persisted Chat History
+    smarthomeDevices?: SmarthomeDevice[];
+    statusLed?: StatusLedConfig;
+    weatherEntityId?: string; // Optional custom weather entity
+    mailConfig?: MailConfig;
+    calDavAccounts?: CalDavConfig[]; // New: Multiple CalDAV accounts
+    showSchoolHolidays?: boolean; // New: School holidays toggle
+    pin?: string;             // 4-digit PIN for profile protection
+    personEntityId?: string;  // HA person entity for presence detection (e.g. person.valentin)
+    permissions?: {
+        allowedZones: string[]; // e.g. ["Wohnung_1", "Gemeinsam"]
+        allowedWorkspaces: string[];
+        isAdmin?: boolean;
+    };
+    layouts?: Record<'mobile' | 'tablet' | 'desktop', {
+        displayOrder: string[];
+        widgetSizes: Record<string, 'small' | 'medium' | 'large'>;
+    }>;
+    themeConfig?: {
+        wallpaper?: string;
+        accentColor?: string;
+        glassOpacity?: number;
+        dockPosition?: { x: number; y: number };
+    };
+}
+
+export type SmarthomeDeviceType = 'light' | 'switch' | 'sensor' | 'cover' | 'climate' | 'washer' | 'dishwasher' | 'pc';
+
+export interface SmarthomeDevice {
+    id: string;
+    name: string;
+    entityId: string;
+    type: SmarthomeDeviceType;
+    zone?: string; // e.g. "Wohnung_1", "Wohnung_2", "Gemeinsam"
+}
+
+export type StatusLedConditionType = 'calendar_event_today' | 'calendar_event_tomorrow' | 'task_keyword' | 'ha_state' | 'ha_state_not';
+
+export interface StatusLedRule {
+    id: string;
+    name: string;
+    type: StatusLedConditionType;
+    conditionValue: string; // e.g. "Müll", "valentin", "sensor.printer:printing"
+    color: string; // hex or rgb
+    enabled: boolean;
+    activeFromTime?: string; // HH:mm format
+    activeToTime?: string; // HH:mm format
+    // Arrays will be evaluated in order, index 0 = highest priority
+}
+
+export interface StatusLedConfig {
+    entityId: string;
+    rules: StatusLedRule[];
+    defaultColor?: string; // Fallback if no rules match
+}
+
+export interface AISettings {
+    enabled: boolean;          // Master switch
+    proactiveHelp: boolean;    // "Soll ich...?" bubbles
+    chatEnabled: boolean;      // The text field
+    contextAwareness: boolean; // Reading calendar/tasks for context
+    provider?: 'local' | 'openai' | 'gemini' | 'openclaw'; // Multi-Cloud Switch
+    cloudApiKey?: string;      // API Key for OpenAI
+    agentId?: string;          // Specific HA Conversation Agent ID
+    geminiApiKey?: string;     // Direct Gemini API Key
+    geminiModel?: string;      // Custom Gemini Model string (e.g. gemini-3.0-flash)
+}
+
+// Removed unused SmarthomeLight interface
+
+export interface SparkSuggestion {
+    action: 'ha_service' | 'add_subtasks' | 'add_reminder' | 'tip' | 'none';
+    message: string;
+    entity_id?: string; // legacy support
+    ha_data?: { domain: string; service: string; entity_id: string; };
+    app_data?: { subtasks?: string[]; reminder_date?: string; reminder_text?: string; };
+    targetId?: string; // e.g. Task ID
+    listId?: string;   // e.g. List ID
+}
+
+export interface ChatMessage {
+    id: string;
+    role: 'user' | 'assistant';
+    text: string;
+    timestamp: string; // Store as string for serialization
+}
+
+export interface MailConfig {
+    enabled: boolean;
+    mailBridgeUrl?: string; // URL of the Node.js mail bridge (e.g. http://192.168.1.100:3001)
+    imapHost?: string;
+    imapPort?: number;
+    imapUser?: string;
+    imapPassword?: string;
+    imapTls?: boolean;
+    smtpHost?: string;
+    smtpPort?: number;
+    smtpUser?: string;
+    smtpPassword?: string;
+    smtpTls?: boolean;
+}
+
+export interface CalDavConfig {
+    enabled: boolean;
+    serverUrl?: string;
+    username?: string;
+    password?: string;
+    color?: string;
+    ownerId?: string; // Which user does this belong to?
+}
+
+export interface Task {
+    id: string;
+    text: string;
+    completed: boolean;
+    energy: number;
+    dueDate?: Date;
+    subtasks?: Task[]; // Nested tasks
+}
+
+export interface TodoList {
+    id: string;
+    ownerId: string; // Who owns this list?
+    sharedWith: string[]; // Who else sees it? ('all' for family)
+    name: string;
+    theme: ThemeColor;
+    tasks: Task[];
+    archivedTasks: Task[];
+}
+
+export interface Habit {
+    id: string;
+    ownerId: string;
+    name: string;
+    icon: string; // lucide icon name or emoji
+    color: ThemeColor;
+    frequency: 'daily' | 'weekly';
+    targetCount: number; // e.g. drink 5 glasses
+    completedDates: Record<string, number>; // Map of 'YYYY-MM-DD' to completion count
+    createdAt: string;
+    notificationsEnabled?: boolean;
+    aiSmartReminders?: boolean; // AI decides when and what to send
+}
+
+export interface HabitSlice {
+    habits: Habit[];
+
+    addHabit: (habit: Omit<Habit, 'id' | 'completedDates' | 'createdAt'>) => void;
+    updateHabit: (id: string, updates: Partial<Habit>) => void;
+    removeHabit: (id: string) => void;
+    logHabit: (id: string, dateStr: string, amount?: number) => void;
+    unlogHabit: (id: string, dateStr: string, amount?: number) => void;
+    getVisibleHabits: () => Habit[];
+    checkSmartHabitReminders: () => Promise<void>;
+}
+
+export interface WorkspaceItem {
+    id: string;
+    name: string;
+    icon: string;
+    color: ThemeColor;
+    type: 'ha_entity' | 'url';
+    value: string; // The URL to open, or the HA Entity ID to trigger
+    zone?: string; // Optional filtering zone
+}
+
+export interface RecipeProfile {
+    id: string; // e.g. "lasagne", lowercase name
+    originalName: string;
+    ingredients: string[]; // Current learned preferred ingredients
+    rejectedIngredients: string[]; // Ingredients the user explicitly removed
+    usageCount: number;
+}
+
+export interface SmartListSlice {
+    recipeProfiles: RecipeProfile[];
+    getRecipeSuggestion: (taskName: string) => RecipeProfile | null;
+    learnRecipePreferences: (recipeId: string, acceptedIngredients: string[], rejectedIngredients: string[]) => void;
+}
+
+export interface WorkspaceSlice {
+    workspaceItems: WorkspaceItem[];
+    addWorkspaceItem: (item: Omit<WorkspaceItem, 'id'>) => void;
+    removeWorkspaceItem: (id: string) => void;
+}
+
+export interface Person {
+    id: string;
+    name: string;
+    color: string;
+}
+
+export type CalendarEvent = {
+    id: string;
+    ownerId: string; // Who created this?
+    sharedWith: string[]; // 'all' or specific user IDs
+    date: Date;
+    endDate?: Date; // For multi-day or duration
+    title: string;
+    description?: string;
+    location?: string;
+    participantIds: string[];
+    color: string;
+    label?: string; // Semantic label (e.g., 'Urlaub', 'Arbeit') for sharing translation
+    time?: string; // HH:MM start
+    endTime?: string; // HH:MM end
+    isAllDay?: boolean;
+    recurrence?: {
+        type: 'none' | 'daily' | 'weekly' | 'biweekly' | 'monthly' | 'yearly' | 'custom';
+        interval?: number; // for custom
+        endDate?: Date; // until when?
+    };
+};
+
+// --- Slices ---
+
+interface UserSlice {
+    users: User[];
+    activeUserId: string;
+    currentUser: () => User;
+    switchUser: (id: string) => void;
+    updateUser: (id: string, updates: Partial<User>) => void;
+    updateUserLayout: (breakpoint: 'mobile' | 'tablet' | 'desktop', order: string[], sizes: Record<string, 'small' | 'medium' | 'large'>) => void;
+    updateAiSettings: (settings: Partial<AISettings>) => void; // New Action
+    updateThemeConfig: (config: Partial<User['themeConfig']>) => void; // New Action
+    updateColorLabel: (userId: string, color: string, label: string) => void;
+    verifyPin: (userId: string, pin: string) => boolean;
+    updateUserPin: (userId: string, newPin: string | undefined) => void;
+    updatePermissions: (userId: string, permissions: Partial<User['permissions']>) => void;
+
+    // Mail Actions
+    updateMailConfig: (userId: string, config: Partial<MailConfig>) => void;
+
+    // Smarthome Actions
+    addSmarthomeDevice: (device: Omit<SmarthomeDevice, 'id'>) => void;
+    updateSmarthomeDevice: (id: string, updates: Partial<SmarthomeDevice>) => void;
+    removeSmarthomeDevice: (id: string) => void;
+    updateWeatherEntityId: (entityId: string | undefined) => void;
+
+    // Status LED Actions
+    updateStatusLedConfig: (config: Partial<StatusLedConfig>) => void;
+    addStatusLedRule: (rule: Omit<StatusLedRule, 'id'>) => void;
+    updateStatusLedRule: (id: string, updates: Partial<StatusLedRule>) => void;
+    removeStatusLedRule: (id: string) => void;
+    reorderStatusLedRules: (startIndex: number, endIndex: number) => void;
+
+    // Status LED Transient State
+    activeStatusLedRule: StatusLedRule | null;
+    statusLedAcknowledgedRuleId: string | null;
+    setActiveStatusLedRule: (rule: StatusLedRule | null) => void;
+    acknowledgeActiveStatusLedRule: () => void;
+    clearStatusLedAcknowledgment: () => void;
+
+    addChatMessage: (userId: string, message: ChatMessage) => void;
+    clearChatHistory: (userId: string) => void;
+    // Admin Actions
+    addUser: (name: string, avatar: string, color: string) => void;
+    removeUser: (id: string) => void;
+    importState: (newState: any) => void;
+    isHydrated: boolean;
+    setHydrated: (state: boolean) => void;
+
+    // AI Global Spark
+    sparkSuggestion: SparkSuggestion | null;
+    showSparkBubble: boolean;
+    setSparkSuggestion: (suggestion: SparkSuggestion | null) => void;
+    setShowSparkBubble: (show: boolean) => void;
+
+    // Persistence Feedback
+    persistenceError: string | null;
+    setPersistenceError: (error: string | null) => void;
+    isSyncing: boolean;
+
+    // Background Sync
+    syncWithHA: (force?: boolean) => Promise<void>;
+
+    // Holiday Integration
+    holidays: CalendarEvent[];
+    isFetchingHolidays: boolean;
+    fetchHolidays: () => Promise<void>;
+
+    // Real-time MQTT/SSE Pipeline
+    latestMqttEvent: { topic: string, payload: string } | null;
+    setLatestMqttEvent: (event: { topic: string, payload: string } | null) => void;
+}
+
+interface ChaosSlice {
+    lists: TodoList[];
+    activeListId: string;
+
+    // Actions
+    addList: (name: string, theme: ThemeColor, sharedWith?: string[]) => void;
+    updateList: (id: string, updates: Partial<TodoList>) => void;
+    removeList: (id: string) => void;
+    selectList: (id: string) => void;
+    addTask: (listId: string, text: string, dueDate?: Date) => void;
+    updateTask: (listId: string, taskId: string, updates: Partial<Task>) => void;
+    moveTask: (currentListId: string, targetListId: string, taskId: string) => void;
+    toggleTask: (listId: string, taskId: string) => void;
+    deleteTask: (listId: string, taskId: string) => void;
+
+    // Admin Cleanup
+    clearAllCompletedTasks: () => void;
+
+    // ... existing actions ...
+
+    // Subtask Actions
+    addSubtask: (listId: string, parentTaskId: string, text: string) => void;
+    toggleSubtask: (listId: string, parentTaskId: string, subtaskId: string) => void;
+    deleteSubtask: (listId: string, parentTaskId: string, subtaskId: string) => void;
+
+    toggleListSharing: (listId: string) => void;
+    archiveTask: (taskId: string) => void;
+    restoreTask: (taskId: string) => void;
+    clearArchive: () => void;
+
+    // Getters
+    activeList: () => TodoList;
+    getVisibleLists: () => TodoList[];
+    completionPercentage: () => number;
+
+    // Focus Mode
+    activeFocusTaskId: string | null;
+    focusStartTime: number | null;
+    focusDurationMinutes: number;
+    startFocusMode: (taskId: string, durationMinutes: number) => void;
+    stopFocusMode: () => void;
+}
+
+
+export interface CalendarSlice {
+    events: CalendarEvent[];
+    people: Person[];
+
+    addEvent: (event: Omit<CalendarEvent, 'id' | 'ownerId'>) => void;
+    updateEvent: (id: string, updates: Partial<CalendarEvent>) => void;
+    importEvents: (events: Partial<CalendarEvent>[]) => void;
+    removeEvent: (id: string) => void;
+    addPerson: (name: string, color: string) => void;
+    removePerson: (id: string) => void;
+    getVisibleEvents: () => CalendarEvent[];
+    syncCalDav: () => Promise<void>;
+}
+
+type StoreState = UserSlice & ChaosSlice & CalendarSlice & HabitSlice & WorkspaceSlice & SmartListSlice;
+
+// --- Implementations ---
+
+// Debug Logger
+console.log("Store File Loading...");
+
+const generateId = () => Math.random().toString(36).substring(2, 9);
+
+const createUserSlice: StateCreator<StoreState, [], [], UserSlice> = (set, get) => ({
+    users: [
+        { id: 'valentin', name: 'Valentin', avatar: '🍆', color: 'text-indigo-400', colorLabels: { 'rose': 'Urlaub', 'amber': 'Arbeit' }, aiSettings: { enabled: true, proactiveHelp: true, chatEnabled: true, contextAwareness: true, provider: 'local' } },
+        { id: 'leon', name: 'Leon', avatar: '🍑', color: 'text-cyan-400', colorLabels: {}, aiSettings: { enabled: false, proactiveHelp: false, chatEnabled: false, contextAwareness: false, provider: 'local' } },
+        { id: 'admin', name: 'Administrator', avatar: '🛡️', color: 'text-rose-500', isHidden: false, pin: '0000' }
+    ],
+    activeUserId: 'valentin', // Default
+    isHydrated: false,
+
+    sparkSuggestion: null,
+    showSparkBubble: false,
+
+    activeStatusLedRule: null,
+    statusLedAcknowledgedRuleId: null,
+
+    persistenceError: null,
+    setPersistenceError: (error) => set({ persistenceError: error }),
+    isSyncing: false,
+
+    holidays: [],
+    isFetchingHolidays: false,
+
+    latestMqttEvent: null,
+    setLatestMqttEvent: (event) => set({ latestMqttEvent: event }),
+
+    fetchHolidays: async () => {
+        if (get().holidays.length > 0) return; // Basic cache
+        set({ isFetchingHolidays: true });
+        try {
+            const response = await fetch('https://ferien-api.de/api/v1/holidays/NI');
+            if (!response.ok) throw new Error('API Error');
+            const data = await response.json();
+
+            const holidayEvents: CalendarEvent[] = data.map((h: any) => ({
+                id: `holiday-${h.slug}-${h.start}`,
+                ownerId: 'system',
+                sharedWith: ['all'],
+                date: new Date(h.start),
+                endDate: new Date(h.end),
+                title: h.name,
+                isAllDay: true,
+                color: 'sky'
+            }));
+
+            set({ holidays: holidayEvents });
+        } catch (e) {
+            console.error('Failed to fetch holidays:', e);
+        } finally {
+            set({ isFetchingHolidays: false });
+        }
+    },
+
+    syncWithHA: async (force = false) => {
+        const state = get();
+        if (!state.isHydrated && !force) return;
+
+        try {
+            set({ isSyncing: true });
+            console.log('[HA Sync] Checking for remote updates...');
+            // Check if remote state is newer
+            const remote = await haService.getState(true);
+            if (!remote) {
+                set({ isSyncing: false });
+                return;
+            }
+
+            const { data: remoteData, last_updated: remoteTimestamp } = remote;
+            const localTimestamp = haService.lastSync;
+
+            // Only merge if remote is strictly newer or if forced
+            if (force || !localTimestamp || remoteTimestamp > localTimestamp) {
+                console.log('[HA Sync] Remote state is newer. Merging...', { remoteTimestamp, localTimestamp });
+
+                // We use set to update the store. Since we are using persist middleware,
+                // this might trigger another save, but the debounce and timestamp check should prevent loops.
+                set((state) => ({
+                    ...state,
+                    ...remoteData,
+                    // Ensure activeListId is still valid after merge
+                    activeListId: remoteData.lists?.find((l: any) => l.id === remoteData.activeListId)
+                        ? remoteData.activeListId
+                        : (remoteData.lists?.[0]?.id || 'default')
+                }));
+
+                haService.lastSync = remoteTimestamp;
+                console.log('[HA Sync] Merge complete.');
+
+                // After local state is updated with HA data, trigger external syncs
+                get().syncCalDav();
+            } else {
+                console.log('[HA Sync] Local state is up to date.');
+            }
+            set({ persistenceError: null, isSyncing: false });
+        } catch (e) {
+            console.error('[HA Sync] Background sync failed', e);
+            set({ isSyncing: false });
+        }
+    },
+
+    setSparkSuggestion: (suggestion) => set({ sparkSuggestion: suggestion }),
+    setShowSparkBubble: (show) => set({ showSparkBubble: show }),
+
+    setActiveStatusLedRule: (rule) => set({ activeStatusLedRule: rule }),
+    acknowledgeActiveStatusLedRule: () => set((state) => ({
+        statusLedAcknowledgedRuleId: state.activeStatusLedRule?.id || null
+    })),
+    clearStatusLedAcknowledgment: () => set({ statusLedAcknowledgedRuleId: null }),
+    setHydrated: (state) => set({ isHydrated: state }),
+
+    currentUser: () => {
+        const state = get();
+        return state.users.find(u => u.id === state.activeUserId) || state.users[0];
+    },
+
+    switchUser: (id) => set({ activeUserId: id }),
+
+    updateUser: (id, updates) => set((state) => ({
+        users: state.users.map(u => u.id === id ? { ...u, ...updates } : u)
+    })),
+
+    updateUserLayout: (breakpoint, order, sizes) => set((state) => ({
+        users: state.users.map(u => {
+            if (u.id !== state.activeUserId) return u;
+            const currentLayouts = u.layouts || {
+                mobile: { displayOrder: u.displayOrder || ['status', 'smarthome', 'focus', 'calendar'], widgetSizes: u.widgetSizes || {} },
+                tablet: { displayOrder: u.displayOrder || ['status', 'smarthome', 'focus', 'calendar'], widgetSizes: u.widgetSizes || {} },
+                desktop: { displayOrder: u.displayOrder || ['status', 'smarthome', 'focus', 'calendar'], widgetSizes: u.widgetSizes || {} },
+            };
+            return {
+                ...u,
+                layouts: {
+                    ...currentLayouts,
+                    [breakpoint]: { displayOrder: order, widgetSizes: sizes }
+                }
+            };
+        })
+    })),
+
+    updateAiSettings: (settings: Partial<AISettings>) => set((state) => ({
+        users: state.users.map(u => u.id === state.activeUserId ? {
+            ...u,
+            aiSettings: { ...u.aiSettings, ...settings } as AISettings
+        } : u)
+    })),
+
+    updateThemeConfig: (config) => set((state) => ({
+        users: state.users.map(u => u.id === state.activeUserId ? {
+            ...u,
+            themeConfig: { ...u.themeConfig, ...config }
+        } : u)
+    })),
+    updateColorLabel: (userId, color, label) => set((state) => ({
+        users: state.users.map(u => u.id === userId
+            ? { ...u, colorLabels: { ...u.colorLabels, [color]: label } }
+            : u
+        )
+    })),
+
+    updateMailConfig: (userId, config) => set((state) => ({
+        users: state.users.map(u => {
+            if (u.id === userId) {
+                return {
+                    ...u,
+                    mailConfig: { ...u.mailConfig, ...config } as MailConfig
+                };
+            }
+            return u;
+        })
+    })),
+
+    addSmarthomeDevice: (device) => set((state) => ({
+        users: state.users.map(u => u.id === state.activeUserId ? {
+            ...u,
+            smarthomeDevices: [...(u.smarthomeDevices || []), { ...device, id: generateId() }]
+        } : u)
+    })),
+
+    updateSmarthomeDevice: (id, updates) => set((state) => ({
+        users: state.users.map(u => u.id === state.activeUserId ? {
+            ...u,
+            smarthomeDevices: (u.smarthomeDevices || []).map(d => d.id === id ? { ...d, ...updates } : d)
+        } : u)
+    })),
+
+    removeSmarthomeDevice: (id) => set((state) => ({
+        users: state.users.map(u => u.id === state.activeUserId ? {
+            ...u,
+            smarthomeDevices: (u.smarthomeDevices || []).filter(d => d.id !== id)
+        } : u)
+    })),
+
+    updateWeatherEntityId: (entityId) => set((state) => ({
+        users: state.users.map(u => u.id === state.activeUserId ? { ...u, weatherEntityId: entityId } : u)
+    })),
+
+    updateStatusLedConfig: (config) => set((state) => ({
+        users: state.users.map(u => u.id === state.activeUserId ? {
+            ...u,
+            statusLed: { ...u.statusLed, ...config } as StatusLedConfig
+        } : u)
+    })),
+
+    addStatusLedRule: (rule) => set((state) => ({
+        users: state.users.map(u => u.id === state.activeUserId ? {
+            ...u,
+            statusLed: {
+                ...u.statusLed,
+                entityId: u.statusLed?.entityId || '',
+                rules: [...(u.statusLed?.rules || []), { ...rule, id: generateId() }]
+            }
+        } : u)
+    })),
+
+    updateStatusLedRule: (id, updates) => set((state) => ({
+        users: state.users.map(u => u.id === state.activeUserId && u.statusLed ? {
+            ...u,
+            statusLed: {
+                ...u.statusLed,
+                rules: u.statusLed.rules.map(r => r.id === id ? { ...r, ...updates } : r)
+            }
+        } : u)
+    })),
+
+    removeStatusLedRule: (id) => set((state) => ({
+        users: state.users.map(u => u.id === state.activeUserId && u.statusLed ? {
+            ...u,
+            statusLed: {
+                ...u.statusLed,
+                rules: u.statusLed.rules.filter(r => r.id !== id)
+            }
+        } : u)
+    })),
+
+    reorderStatusLedRules: (startIndex, endIndex) => set((state) => ({
+        users: state.users.map(u => {
+            if (u.id !== state.activeUserId || !u.statusLed) return u;
+            const result = Array.from(u.statusLed.rules);
+            const [removed] = result.splice(startIndex, 1);
+            result.splice(endIndex, 0, removed);
+            return {
+                ...u,
+                statusLed: {
+                    ...u.statusLed,
+                    rules: result
+                }
+            }
+        })
+    })),
+
+    addChatMessage: (userId, message) => set((state) => ({
+        users: state.users.map(u => u.id === userId ? {
+            ...u,
+            chatHistory: [...(u.chatHistory || []), message]
+        } : u)
+    })),
+
+    clearChatHistory: (userId) => set((state) => ({
+        users: state.users.map(u => u.id === userId ? {
+            ...u,
+            chatHistory: []
+        } : u)
+    })),
+
+
+
+
+
+    addUser: (name, avatar, color) => set((state) => ({
+        users: [...state.users, {
+            id: generateId(),
+            name,
+            avatar,
+            color,
+            colorLabels: {}
+        }]
+    })),
+
+    removeUser: (id) => set((state) => ({
+        users: state.users.filter(u => u.id !== id)
+    })),
+
+    importState: (newState) => set(() => ({
+        ...newState
+    })),
+
+    verifyPin: (userId, pin) => {
+        const user = get().users.find(u => u.id === userId);
+        if (!user || !user.pin) return true; // No PIN = access granted
+        return user.pin === pin;
+    },
+
+    updateUserPin: (userId, newPin) => set((state) => ({
+        users: state.users.map(u => u.id === userId ? { ...u, pin: newPin } : u)
+    })),
+
+    updatePermissions: (userId, permissions) => set((state) => ({
+        users: state.users.map(u => u.id === userId ? {
+            ...u,
+            permissions: { ...u.permissions, ...permissions } as any
+        } : u)
+    }))
+});
+
+const createChaosSlice: StateCreator<StoreState, [], [], ChaosSlice> = (set, get) => ({
+    lists: [
+        { id: 'default', ownerId: 'valentin', sharedWith: [], name: 'Meine Aufgaben', theme: 'scifi', tasks: [], archivedTasks: [] },
+        { id: 'fam-1', ownerId: 'valentin', sharedWith: ['all'], name: 'Familien Kram', theme: 'orange', tasks: [], archivedTasks: [] }
+    ],
+    activeListId: 'default',
+
+    activeFocusTaskId: null,
+    focusStartTime: null,
+    focusDurationMinutes: 25,
+
+    startFocusMode: (taskId, durationMinutes) => set({
+        activeFocusTaskId: taskId,
+        focusStartTime: Date.now(),
+        focusDurationMinutes: durationMinutes
+    }),
+
+    stopFocusMode: () => set({
+        activeFocusTaskId: null,
+        focusStartTime: null
+    }),
+
+    clearAllCompletedTasks: () => set((state) => ({
+        lists: state.lists.map(list => ({
+            ...list,
+            tasks: list.tasks.filter(t => !t.completed),
+            // Optional: Move them to archive instead of deleting? For now, implementing "Delete" as requested.
+            archivedTasks: [...list.archivedTasks, ...list.tasks.filter(t => t.completed)]
+        }))
+    })),
+
+    addList: (name, theme, sharedWith) => set((state) => ({
+        lists: [...state.lists, {
+            id: generateId(),
+            ownerId: state.activeUserId,
+            sharedWith: sharedWith || [], // Private or specified
+            name,
+            theme,
+            tasks: [],
+            archivedTasks: []
+        }]
+    })),
+
+    updateList: (id, updates) => set((state) => ({
+        lists: state.lists.map(l => l.id === id ? { ...l, ...updates } : l)
+    })),
+
+    removeList: (id) => set((state) => ({
+        lists: state.lists.filter(l => l.id !== id),
+        activeListId: state.activeListId === id ? 'default' : state.activeListId
+    })),
+
+    selectList: (id) => set({ activeListId: id }),
+
+    addTask: (listId, text, dueDate) => set((state) => ({
+        lists: state.lists.map(list =>
+            list.id === listId
+                ? { ...list, tasks: [...list.tasks, { id: generateId(), text, completed: false, energy: 100, dueDate }] }
+                : list
+        )
+    })),
+
+    updateTask: (listId, taskId, updates) => set((state) => ({
+        lists: state.lists.map(list => list.id === listId
+            ? {
+                ...list,
+                tasks: list.tasks.map(task => task.id === taskId
+                    ? { ...task, ...updates }
+                    : task
+                )
+            }
+            : list
+        )
+    })),
+
+    moveTask: (currentListId, targetListId, taskId) => set((state) => {
+        const sourceList = state.lists.find(l => l.id === currentListId);
+        const task = sourceList?.tasks.find(t => t.id === taskId);
+        if (!sourceList || !task) return state;
+
+        return {
+            lists: state.lists.map(list => {
+                if (list.id === currentListId) {
+                    return { ...list, tasks: list.tasks.filter(t => t.id !== taskId) };
+                }
+                if (list.id === targetListId) {
+                    return { ...list, tasks: [...list.tasks, task] };
+                }
+                return list;
+            })
+        };
+    }),
+
+    toggleTask: (listId, taskId) => set((state) => ({
+        lists: state.lists.map((l) =>
+            l.id === listId
+                ? {
+                    ...l,
+                    tasks: l.tasks.map((t) =>
+                        t.id === taskId ? { ...t, completed: !t.completed } : t
+                    ),
+                }
+                : l
+        ),
+    })),
+
+    deleteTask: (listId, taskId) => set((state) => ({
+        lists: state.lists.map((l) =>
+            l.id === listId
+                ? { ...l, tasks: l.tasks.filter((t) => t.id !== taskId) }
+                : l
+        ),
+    })),
+
+    // Subtask Implementation
+    addSubtask: (listId, parentTaskId, text) => set((state) => ({
+        lists: state.lists.map(l =>
+            l.id === listId
+                ? {
+                    ...l,
+                    tasks: l.tasks.map(t =>
+                        t.id === parentTaskId
+                            ? { ...t, subtasks: [...(t.subtasks || []), { id: generateId(), text, completed: false, energy: 0 }] }
+                            : t
+                    )
+                }
+                : l
+        )
+    })),
+
+    toggleSubtask: (listId, parentTaskId, subtaskId) => set((state) => ({
+        lists: state.lists.map(l =>
+            l.id === listId
+                ? {
+                    ...l,
+                    tasks: l.tasks.map(t =>
+                        t.id === parentTaskId && t.subtasks
+                            ? {
+                                ...t,
+                                subtasks: t.subtasks.map(st =>
+                                    st.id === subtaskId ? { ...st, completed: !st.completed } : st
+                                )
+                            }
+                            : t
+                    )
+                }
+                : l
+        )
+    })),
+
+    deleteSubtask: (listId, parentTaskId, subtaskId) => set((state) => ({
+        lists: state.lists.map(l =>
+            l.id === listId
+                ? {
+                    ...l,
+                    tasks: l.tasks.map(t =>
+                        t.id === parentTaskId && t.subtasks
+                            ? {
+                                ...t,
+                                subtasks: t.subtasks.filter(st => st.id !== subtaskId)
+                            }
+                            : t
+                    )
+                }
+                : l
+        )
+    })),
+
+    // NEW: Toggle Sharing
+    toggleListSharing: (listId) => set((state) => ({
+        lists: state.lists.map(l => {
+            if (l.id !== listId) return l;
+            // Toggle logic: If Shared with 'family', remove it. If not, add it.
+            // Simplified: We just check if it has entries.
+            // Ideally we'd select WHO to share with, but for now "Shared" means "Visible to everyone/Family"
+            // Let's assume a special ID 'family' or just checking if sharedWith is empty.
+
+            const isShared = l.sharedWith.length > 0;
+            return {
+                ...l,
+                sharedWith: isShared ? [] : ['family'] // Simple toggle
+            };
+        })
+    })),
+
+    getVisibleLists: () => {
+        const state = get();
+        const activeId = state.activeUserId;
+        // Show if Owner OR Shared
+        return state.lists.filter(l =>
+            l.ownerId === activeId ||
+            (l.sharedWith && l.sharedWith.length > 0) // Simplified: If shared with anyone, show it (or specifically 'family')
+        );
+    },
+
+    archiveTask: (taskId) => set((state) => {
+        const list = state.lists.find(l => l.id === state.activeListId);
+        if (!list) return state;
+
+        const taskToArchive = list.tasks.find(t => t.id === taskId);
+        if (!taskToArchive) return state;
+
+        return {
+            lists: state.lists.map(l =>
+                l.id === state.activeListId
+                    ? {
+                        ...l,
+                        tasks: l.tasks.filter(t => t.id !== taskId),
+                        archivedTasks: [...l.archivedTasks, taskToArchive]
+                    }
+                    : l
+            )
+        };
+    }),
+
+    restoreTask: (taskId) => set((state) => {
+        const list = state.lists.find(l => l.id === state.activeListId);
+        if (!list) return state;
+
+        const taskToRestore = list.archivedTasks.find(t => t.id === taskId);
+        if (!taskToRestore) return state;
+
+        return {
+            lists: state.lists.map(l =>
+                l.id === state.activeListId
+                    ? {
+                        ...l,
+                        archivedTasks: l.archivedTasks.filter(t => t.id !== taskId),
+                        tasks: [...l.tasks, taskToRestore]
+                    }
+                    : l
+            )
+        };
+    }),
+
+    clearArchive: () => set((state) => ({
+        lists: state.lists.map(l =>
+            l.id === state.activeListId
+                ? { ...l, archivedTasks: [] }
+                : l
+        )
+    })),
+
+    activeList: () => {
+        const state = get();
+        return state.lists.find(l => l.id === state.activeListId) || state.lists[0];
+    },
+
+    // getVisibleLists is already defined above at line 220. Removing duplicate.
+
+    completionPercentage: () => {
+        const list = get().activeList();
+        if (list.tasks.length === 0) return 0;
+
+        const countTasks = (tasks: Task[]): { total: number; completed: number } => {
+            return tasks.reduce((acc, task) => {
+                const sub = task.subtasks ? countTasks(task.subtasks) : { total: 0, completed: 0 };
+                return {
+                    total: acc.total + 1 + sub.total,
+                    completed: acc.completed + (task.completed ? 1 : 0) + sub.completed
+                };
+            }, { total: 0, completed: 0 });
+        };
+
+        const stats = countTasks(list.tasks);
+        if (stats.total === 0) return 0;
+        return (stats.completed / stats.total) * 100;
+    }
+});
+
+export const createCalendarSlice: StateCreator<StoreState, [], [], CalendarSlice> = (set, get) => {
+    console.log("Initializing Calendar Slice...");
+    return {
+        events: [],
+        people: [
+            { id: 'p1', name: 'Valentin', color: 'indigo' },
+            { id: 'p2', name: 'Leon', color: 'cyan' }
+        ],
+
+        addEvent: (event) => set((state) => ({
+            events: [...state.events, {
+                ...event,
+                id: generateId(),
+                ownerId: state.activeUserId,
+                sharedWith: event.sharedWith || [] // Allow passing sharedWith on creation
+            }]
+        })),
+
+        updateEvent: (id, updates) => set((state) => ({
+            events: state.events.map(e => e.id === id ? { ...e, ...updates } : e)
+        })),
+
+        importEvents: (newEvents) => set((state) => ({
+            events: [...state.events, ...newEvents.map(e => ({
+                ...e,
+                id: generateId(), // Ensure new ID
+                ownerId: e.ownerId || state.activeUserId, // Fallback to active user
+                participantIds: e.participantIds || [],
+                sharedWith: e.sharedWith || [],
+                recurrence: e.recurrence || { type: 'none' }
+            } as CalendarEvent))]
+        })),
+
+        removeEvent: (id) => set((state) => ({
+            events: state.events.filter((e) => e.id !== id)
+        })),
+
+        addPerson: (name, color) => set((state) => ({
+            people: [...state.people, { id: generateId(), name, color }]
+        })),
+
+        removePerson: (id) => set((state) => ({
+            people: state.people.filter((p) => p.id !== id)
+        })),
+
+        getVisibleEvents: () => {
+            const state = get();
+            const baseEvents = state.events.filter(e =>
+                e.ownerId === state.activeUserId ||
+                e.sharedWith.includes('all') ||
+                e.sharedWith.includes(state.activeUserId)
+            );
+
+            // Expand recurring events for the current month/view (heuristic: next 3 months)
+            const expandedEvents: CalendarEvent[] = [];
+            const now = new Date();
+            const horizon = new Date();
+            horizon.setMonth(now.getMonth() + 3);
+
+            baseEvents.forEach(event => {
+                expandedEvents.push(event); // Always include the base event
+
+                if (event.recurrence && event.recurrence.type !== 'none') {
+                    let current = new Date(event.date);
+                    const type = event.recurrence.type;
+
+                    while (true) {
+                        if (type === 'daily') current.setDate(current.getDate() + 1);
+                        else if (type === 'weekly') current.setDate(current.getDate() + 7);
+                        else if (type === 'biweekly') current.setDate(current.getDate() + 14);
+                        else if (type === 'monthly') current.setMonth(current.getMonth() + 1);
+                        else if (type === 'yearly') current.setFullYear(current.getFullYear() + 1);
+                        else break;
+
+                        if (current > horizon) break;
+
+                        expandedEvents.push({
+                            ...event,
+                            id: `${event.id}-rec-${current.getTime()}`,
+                            date: new Date(current),
+                            endDate: event.endDate ? new Date(current.getTime() + (new Date(event.endDate).getTime() - new Date(event.date).getTime())) : undefined
+                        });
+                    }
+                }
+            });
+
+            return expandedEvents;
+        },
+
+        syncCalDav: async () => {
+            const state = get();
+            // Collect all enabled accounts from all users
+            const accounts = state.users.flatMap(u =>
+                (u.calDavAccounts || []).map(a => ({ ...a, ownerId: u.id }))
+            ).filter(a => a.enabled);
+
+            if (accounts.length === 0) return;
+
+            try {
+                const response = await fetch('/api/caldav/sync', {
+                    method: 'POST',
+                    headers: { 'Content-Type': 'application/json' },
+                    body: JSON.stringify({ accounts })
+                });
+                const data = await response.json();
+                if (data.success) {
+                    if (data.events) {
+                        // Map string dates back to Date objects
+                        const syncedEvents = data.events.map((e: any) => ({
+                            ...e,
+                            date: new Date(e.date),
+                            endDate: e.endDate ? new Date(e.endDate) : undefined,
+                            sharedWith: e.sharedWith || [],
+                            participantIds: e.participantIds || []
+                        }));
+
+                        // Heuristic: filter out old events that have IDs looking like CalDAV URLs
+                        // or just keep manual events (those without a slash in ID or something similar)
+                        const manualEvents = state.events.filter(e => !e.id.startsWith('http') && !e.id.includes('.ics'));
+
+                        set({ events: [...manualEvents, ...syncedEvents] });
+                        console.log(`[CalDAV Sync] Successfully synced ${syncedEvents.length} events.`);
+                    }
+
+                    if (data.tasks && data.tasks.length > 0) {
+                        const syncedTasksParams = data.tasks.map((t: any) => ({
+                            ...t,
+                            dueDate: t.dueDate ? new Date(t.dueDate) : undefined
+                        }));
+
+                        // We need to group tasks by listName (mapped to calendar displayName)
+                        set((currentState) => {
+                            const newLists = [...currentState.lists];
+
+                            syncedTasksParams.forEach((st: any) => {
+                                // Find if a list exists with this name
+                                let listIndex = newLists.findIndex(l => l.name === st.listName);
+
+                                // Create list if it doesn't exist
+                                if (listIndex === -1) {
+                                    newLists.push({
+                                        id: 'caldav-' + st.listName,
+                                        ownerId: st.ownerId,
+                                        sharedWith: ['all'],
+                                        name: st.listName,
+                                        theme: 'blue',
+                                        tasks: [],
+                                        archivedTasks: []
+                                    });
+                                    listIndex = newLists.length - 1;
+                                }
+
+                                // Prevent duplicates by checking ID
+                                const list = newLists[listIndex];
+                                const existingTaskIdx = list.tasks.findIndex(lt => lt.id === st.id);
+
+                                if (existingTaskIdx !== -1) {
+                                    // Update existing
+                                    list.tasks[existingTaskIdx] = { ...list.tasks[existingTaskIdx], ...st };
+                                } else if (!list.archivedTasks.find(at => at.id === st.id)) {
+                                    // Add new if not archived
+                                    list.tasks.push({
+                                        id: st.id,
+                                        text: st.text,
+                                        completed: st.completed,
+                                        energy: st.energy,
+                                        dueDate: st.dueDate,
+                                        subtasks: []
+                                    });
+                                }
+                            });
+
+                            return { lists: newLists };
+                        });
+                        console.log(`[CalDAV Sync] Successfully synced ${data.tasks.length} tasks.`);
+                    }
+                }
+            } catch (e) {
+                console.error('[CalDAV Sync] Failed:', e);
+            }
+        }
+    };
+};
+
+export const createHabitSlice: StateCreator<StoreState, [], [], HabitSlice> = (set, get) => ({
+    habits: [],
+
+    addHabit: (habit) => set((state) => ({
+        habits: [...state.habits, {
+            ...habit,
+            id: generateId(),
+            completedDates: {},
+            createdAt: new Date().toISOString()
+        }]
+    })),
+
+    updateHabit: (id, updates) => set((state) => ({
+        habits: state.habits.map(h => h.id === id ? { ...h, ...updates } : h)
+    })),
+
+    removeHabit: (id) => set((state) => ({
+        habits: state.habits.filter(h => h.id !== id)
+    })),
+
+    logHabit: (id, dateStr, amount = 1) => set((state) => ({
+        habits: state.habits.map(h => {
+            if (h.id !== id) return h;
+            const current = h.completedDates[dateStr] || 0;
+            return {
+                ...h,
+                completedDates: {
+                    ...h.completedDates,
+                    [dateStr]: current + amount
+                }
+            };
+        })
+    })),
+
+    unlogHabit: (id, dateStr, amount = 1) => set((state) => ({
+        habits: state.habits.map(h => {
+            if (h.id !== id) return h;
+            const current = h.completedDates[dateStr] || 0;
+            const next = Math.max(0, current - amount);
+
+            const newDates = { ...h.completedDates };
+            if (next === 0) {
+                delete newDates[dateStr];
+            } else {
+                newDates[dateStr] = next;
+            }
+
+            return {
+                ...h,
+                completedDates: newDates
+            };
+        })
+    })),
+
+    getVisibleHabits: () => {
+        const state = get();
+        return state.habits.filter(h => h.ownerId === state.activeUserId);
+    },
+
+    checkSmartHabitReminders: async () => {
+        const state = get();
+        const activeUser = state.currentUser();
+        if (!activeUser?.aiSettings?.enabled) return;
+
+        // Check time: Only between 18:00 and 23:30
+        const now = new Date();
+        const hour = now.getHours();
+        const min = now.getMinutes();
+        const timeVal = hour * 100 + min;
+        if (timeVal < 1800 || timeVal > 2330) return;
+
+        const todayStr = now.toISOString().split('T')[0];
+        const lastReminderKey = `last_ai_habit_reminder_${activeUser.id}`;
+        if (localStorage.getItem(lastReminderKey) === todayStr) return;
+
+        const habitsToCheck = state.habits.filter(h =>
+            h.ownerId === activeUser.id &&
+            h.aiSmartReminders &&
+            (h.completedDates[todayStr] || 0) < h.targetCount
+        );
+
+        if (habitsToCheck.length === 0) return;
+
+        const prompt = `Du bist ein motivierender Life-Coach. 
+Der Nutzer hat folgende Gewohnheiten für heute noch nicht abgeschlossen:
+${habitsToCheck.map(h => `- ${h.name} (${h.completedDates[todayStr] || 0}/${h.targetCount})`).join('\n')}
+
+Schreibe EINE kurze, knackige und motivierende Nachricht (max 2 Sätze), die den Nutzer daran erinnert, diese Ziele noch zu erreichen. Nutze einen leicht sarkastischen aber liebevollen Ton, passend zum "Life OS" Stil. Antworte NUR mit der Nachricht.`;
+
+        try {
+            const { ollamaService } = await import('../utils/ollamaService');
+            const message = await ollamaService.generateContent(prompt);
+
+            if (message && message.trim().length > 5) {
+                const { sendNotification } = await import('../utils/notifications');
+                sendNotification("KI Motivations-Schub 🚀", message.trim());
+                localStorage.setItem(lastReminderKey, todayStr);
+            }
+        } catch (e) {
+            console.error("Habit AI Error", e);
+        }
+    }
+});
+
+export const createWorkspaceSlice: StateCreator<StoreState, [], [], WorkspaceSlice> = (set) => ({
+    workspaceItems: [
+        { id: 'ws1', name: 'Home Assistant', icon: 'home', color: 'blue', type: 'url', value: 'http://homeassistant.local:8123' },
+        { id: 'ws2', name: 'Musik', icon: 'music', color: 'purple', type: 'url', value: 'https://open.spotify.com' },
+        { id: 'ws3', name: 'Kino Modus', icon: 'film', color: 'rose', type: 'ha_entity', value: 'scene.kino_modus' },
+        { id: 'ws4', name: 'Alles Aus', icon: 'power', color: 'rose', type: 'ha_entity', value: 'script.alles_aus' }
+    ],
+    addWorkspaceItem: (item) => set((state) => ({
+        workspaceItems: [...state.workspaceItems, { ...item, id: generateId() }]
+    })),
+    removeWorkspaceItem: (id) => set((state) => ({
+        workspaceItems: state.workspaceItems.filter(i => i.id !== id)
+    }))
+});
+
+// Default initial recipes to bootstrap the system
+const DEFAULT_RECIPES: RecipeProfile[] = [
+    { id: 'lasagne', originalName: 'Lasagne', ingredients: ['Hackfleisch', 'Tomatensoße', 'Lasagneplatten', 'Käse', 'Creme Fraiche', 'Zwiebeln', 'Möhren'], rejectedIngredients: [], usageCount: 0 },
+    { id: 'pfannkuchen', originalName: 'Pfannkuchen', ingredients: ['Mehl', 'Milch', 'Eier', 'Zucker', 'Salz', 'Butter'], rejectedIngredients: [], usageCount: 0 },
+    { id: 'pizza', originalName: 'Pizza', ingredients: ['Pizzateig', 'Tomatensoße', 'Mozzarella', 'Basilikum'], rejectedIngredients: [], usageCount: 0 }
+];
+
+export const createSmartListSlice: StateCreator<StoreState, [], [], SmartListSlice> = (set, get) => ({
+    recipeProfiles: DEFAULT_RECIPES,
+
+    getRecipeSuggestion: (taskName: string) => {
+        if (!taskName) return null;
+        const normalized = taskName.trim().toLowerCase();
+
+        // Exact match first
+        const profile = get().recipeProfiles.find(r => r.id === normalized);
+        if (profile) return profile;
+
+        // Partial match (e.g. "Lasagne einkaufen")
+        const partial = get().recipeProfiles.find(r => normalized.includes(r.id));
+        if (partial) return partial;
+
+        return null; // Return null if it's not a known recipe
+    },
+
+    learnRecipePreferences: (recipeId: string, acceptedIngredients: string[], rejectedIngredients: string[]) => set((state) => {
+        const existingProfiles = state.recipeProfiles || [];
+        const existingIndex = existingProfiles.findIndex(r => r.id === recipeId);
+
+        if (existingIndex >= 0) {
+            // Update existing profile
+            const profile = existingProfiles[existingIndex];
+
+            // Add new accepted ingredients, remove if they were previously rejected
+            const newIngredients = Array.from(new Set([...profile.ingredients, ...acceptedIngredients]))
+                .filter(i => !rejectedIngredients.includes(i)); // Ensure current rejections are honored
+
+            // Add new rejected ingredients
+            const newRejected = Array.from(new Set([...profile.rejectedIngredients, ...rejectedIngredients]));
+
+            const updatedProfiles = [...existingProfiles];
+            updatedProfiles[existingIndex] = {
+                ...profile,
+                ingredients: newIngredients,
+                rejectedIngredients: newRejected,
+                usageCount: profile.usageCount + 1
+            };
+
+            return { recipeProfiles: updatedProfiles };
+        } else {
+            // Should not happen normally if they only click on suggestions, but just in case
+            return state;
+        }
+    })
+});
+
+// --- Sync Logic ---
+import { haService } from '../utils/haService';
+import { debounce } from 'lodash';
+
+// Custom Storage Engine for Zustand that uses HA
+const haStorage: StateStorage = {
+    getItem: async (name: string): Promise<string | null> => {
+        try {
+            console.log(`[HA Storage] Loading ${name} from Home Assistant...`);
+            const data = await haService.getState();
+
+            // If we successfully get something (even null), clear previous errors
+            useAppStore.getState().setPersistenceError(null);
+
+            if (data && data[name]) {
+                console.log(`[HA Storage] Found data for ${name}`);
+                return JSON.stringify(data[name]); // Zustand expects a JSON string
+            }
+            console.log(`[HA Storage] No data found for ${name} in HA.`);
+            return null;
+        } catch (e) {
+            console.error(`[HA Storage] Failed to load state from HA`, e);
+            useAppStore.getState().setPersistenceError(e instanceof Error ? e.message : 'Fehler beim Laden von Home Assistant');
+            return null; // Return null to let Zustand use defaults
+        }
+    },
+    setItem: async (name: string, value: string): Promise<void> => {
+        // Zustand already provides 'value' as a JSON string.
+        console.log(`[HA Storage] Debouncing save for ${name}...`);
+        debouncedSaveToHA(name, value);
+    },
+    removeItem: async (name: string): Promise<void> => {
+        try {
+            console.log(`[HA Storage] Removing ${name} from HA... (Resetting to empty state)`);
+            await haService.saveState({ [name]: null }); // Effectively clears it
+            useAppStore.getState().setPersistenceError(null);
+        } catch (e) {
+            console.error(`[HA Storage] Failed to remove state from HA`, e);
+            useAppStore.getState().setPersistenceError('Fehler beim Löschen in Home Assistant');
+        }
+    },
+};
+
+// Debounced actual save function to prevent spamming the HA API
+const debouncedSaveToHA = debounce(async (name: string, value: string) => {
+    try {
+        console.log(`[HA Storage] Executing save to HA for ${name}`);
+        const currentHaState = await haService.getState() || {};
+        const parsedValue = JSON.parse(value);
+
+        // Merge with existing HA state in case we store other things there later
+        const newState = {
+            ...currentHaState,
+            [name]: parsedValue
+        };
+
+        await haService.saveState(newState);
+        console.log(`[HA Storage] Save complete.`);
+        useAppStore.getState().setPersistenceError(null); // Clear error on success
+    } catch (e) {
+        console.error(`[HA Storage] Failed to save state to HA`, e);
+        useAppStore.getState().setPersistenceError(e instanceof Error ? e.message : 'Fehler beim Speichern in Home Assistant');
+    }
+}, 2000);
+
+export const useAppStore = create<StoreState>()(
+    persist(
+        (set, get, api) => {
+            const user = createUserSlice(set, get, api);
+            const chaos = createChaosSlice(set, get, api);
+            const calendar = createCalendarSlice(set, get, api);
+            const habits = createHabitSlice(set, get, api);
+            const workspaces = createWorkspaceSlice(set, get, api);
+            const smartLists = createSmartListSlice(set, get, api);
+
+            return {
+                ...user,
+                ...chaos,
+                ...calendar,
+                ...habits,
+                ...workspaces,
+                ...smartLists,
+                // Hydration happens automatically via persist middleware now
+                isHydrated: false,
+                setHydrated: (state) => set({ isHydrated: state })
+            };
+        },
+        {
+            name: 'life-os-store', // The key used in HA state
+            storage: createJSONStorage(() => haStorage),
+            onRehydrateStorage: () => (state) => {
+                console.log('[HA Storage] Hydration started');
+                if (state) {
+                    state.setHydrated(true);
+                    console.log('[HA Storage] Hydration finished successfully');
+                }
+            },
+            // Define exactly what gets synced to HA
+            partialize: (state) => ({
+                users: state.users,
+                activeUserId: state.activeUserId,
+                lists: state.lists,
+                activeListId: state.activeListId,
+                events: state.events,
+                people: state.people,
+                habits: state.habits,
+                workspaceItems: state.workspaceItems,
+                recipeProfiles: state.recipeProfiles // Persist learning data!
+            }),
+        }
+    )
+);
+
+console.log("Store File Loaded with HA Persist configuration.");
